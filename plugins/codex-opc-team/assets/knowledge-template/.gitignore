# Derived provider state belongs in PLUGIN_DATA, never in canonical knowledge.
.mem0/
.cache/
*.sqlite
*.sqlite3
*.db

# Raw event streams are runtime data. Preserve only the empty directory marker.
evaluations/events/*
!evaluations/events/.gitkeep
